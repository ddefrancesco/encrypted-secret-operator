---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: generatedsecrets.security.copsds.com
spec:
  group: security.copsds.com
  names:
    kind: GeneratedSecret
    listKind: GeneratedSecretList
    plural: generatedsecrets
    singular: generatedsecret
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              endpoint:
                description: endpoint API generazione
                properties:
                  address:
                    description: |-
                      URL for HTTP endpoints (e.g., "https://crypto.example.com/encrypt")
                      Address for gRPC endpoints (e.g., "crypto.example.com:443")
                    type: string
                  insecure:
                    description: 'Optional: insecure connection (skip TLS verification)'
                    type: boolean
                  protocol:
                    description: 'Protocol: "http" or "grpc"'
                    type: string
                required:
                - address
                - protocol
                type: object
              maxVersions:
                description: versioning
                type: integer
              parameters:
                additionalProperties:
                  type: string
                description: parametri generazione
                type: object
              rotationInterval:
                description: rotazione
                type: string
              trigger:
                description: trigger evento
                properties:
                  onCreate:
                    description: eventi supportati
                    type: boolean
                  onRotate:
                    type: boolean
                  onSpecChange:
                    type: boolean
                  schedule:
                    description: 'opzionale: cron'
                    type: string
                type: object
              type:
                description: tipo di segreto da generare
                type: string
            required:
            - endpoint
            - trigger
            - type
            type: object
          status:
            properties:
              currentVersion:
                type: integer
              lastGeneration:
                format: date-time
                type: string
              observedHash:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
