---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: encryptedsecrets.security.copsds.com
spec:
  group: security.copsds.com
  names:
    kind: EncryptedSecret
    listKind: EncryptedSecretList
    plural: encryptedsecrets
    singular: encryptedsecret
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EncryptedSecret is the Schema for the encryptedsecrets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EncryptedSecretSpec defines the desired state of EncryptedSecret
            properties:
              cryptoEndpoint:
                description: endpoint servizio cifratura
                type: string
              data:
                additionalProperties:
                  type: string
                description: dati in chiaro
                type: object
              rotationInterval:
                description: rotazione automatica
                type: string
              ttl:
                description: TTL
                type: string
            required:
            - cryptoEndpoint
            type: object
          status:
            description: EncryptedSecretStatus defines the observed state of EncryptedSecret
            properties:
              lastRotation:
                format: date-time
                type: string
              phase:
                type: string
              secretName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
